/** Precomputed exponent table for GF(256). 255 entries. */
export declare const EXP_TABLE: number[];

/** Precomputed log table for GF(256). 256 entries. */
export declare const LOG_TABLE: number[];
